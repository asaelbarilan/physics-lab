import { astronomyTopics } from '../../data/astronomy';
export default function AstronomyCatalog({onOpen}:{onOpen:(id:'star-formation'|'black-hole')=>void}) {
  return <section className="astronomy-catalog"><div className="intro"><div><div className="eyebrow">אסטרונומיה / חוקרים את היקום</div><h1>מענני גז ועד חורים שחורים</h1><p>מתחילים בשתי מעבדות: משנים, מתבוננים ובודקים מה הבנו.</p></div><span className="module-badge">2 סימולציות פעילות</span></div><div className="astronomy-cards">{astronomyTopics.filter(t=>t.available).map(t=><button className={'astronomy-card '+t.id} key={t.id} onClick={()=>onOpen(t.id as 'star-formation'|'black-hole')}><span className="astronomy-symbol" aria-hidden="true">{t.icon}</span><span className="eyebrow">סימולציה + התנסות + 3 שאלות</span><h2>{t.title}</h2><p>{t.result}</p><span className="card-controls">{t.controls}</span><strong>למעבדה ←</strong></button>)}</div><div className="catalog-note"><span>כלול גם במעבדת החור השחור:</span><button onClick={()=>onOpen('black-hole')}>מחשבון רדיוס אופק האירועים — 5, 10 ו־100 מסות שמש ←</button></div><details className="planned-topics panel"><summary>הנושאים הבאים במעבדה — עדיין בפיתוח מתוכנן</summary><p>זו מפת ההמשך שביקשת. הכרטיסים להלן עדיין אינם סימולציות פעילות.</p><div className="planned-grid">{astronomyTopics.filter(t=>!t.available&&t.id!=='calculator').map(t=><article key={t.id}><span aria-hidden="true">{t.icon}</span><h3>{t.title}</h3><p>{t.controls}</p><small>{t.result}</small></article>)}</div></details></section>;
}

